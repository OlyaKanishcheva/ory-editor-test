import { Editor } from 'slate';
import { OptionsFormat } from './options';
/**
 * The core of the plugin, which does not relies on `slate-react`, and includes
 * everything but behavior and rendering logic.
 */
declare function core(optsParam: OptionsFormat): {
    schema: Object;
    utils: {
        isSelectionInBlockquote: (value: import("slate").Value) => boolean;
    };
    changes: {
        wrapInBlockquote: (editor: Editor) => Editor;
        unwrapBlockquote: (_editor: Editor) => Editor;
    };
};
export default core;
//# sourceMappingURL=core.d.ts.map