import { Block, Value } from 'slate';
import Options from '../options';
/**
 * Return the current blockquote, from current selection or from a node.
 */
declare const getCurrentBlockquote: (opts: Options) => (value: Value, block?: Block) => Block;
export default getCurrentBlockquote;
//# sourceMappingURL=getCurrentBlockquote.d.ts.map