import { Editor } from 'slate';
import Options from '../options';
import { NextType } from '@guestbell/slate-common/lib/types/next';
/**
 * User pressed Enter in an editor
 *
 * Enter on an empty block inside a blockquote exit the blockquote.
 */
declare function onEnter(event: React.KeyboardEvent, editor: Editor, opts: Options, next: NextType): boolean;
export default onEnter;
//# sourceMappingURL=onEnter.d.ts.map