import { Editor } from 'slate';
import Options from '../options';
import { NextType } from '@guestbell/slate-common/lib/types/next';
/**
 * User pressed Delete in an editor:
 * Unwrap the blockquote if at the start of the inner block.
 */
declare function onBackspace(event: React.KeyboardEvent, editor: Editor, opts: Options, next: NextType): boolean;
export default onBackspace;
//# sourceMappingURL=onBackspace.d.ts.map